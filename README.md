# Multi-Architecture-Image-Classification
Compares Vision Transformer, Hybrid CNN-MLP, and ResNet transfer learning on CIFAR-10 classification. Implements custom ViT with patch embedding, CNN feature extraction with MLP head, and fine-tuned ResNet18 with comprehensive performance analysis.
